---
version: "0.1.0"
date: 2026-06-14
lang: en
kind: minor
summary: First working apply across a static inventory.
---

### Added
- `host add`, `plan` and `apply` over SSH, against a static inventory file.
- Desired and observed state in PostgreSQL, with an append-only audit log.
- systemd unit generation from templates.

---
import ProjectRoadmap from '../../../../../components/pages/ProjectRoadmap.astro';
import { sectionPaths } from '../../../../../lib/projects';

export async function getStaticPaths() {
  return sectionPaths('tr', 'roadmap');
}

const { project } = Astro.props;
---
<ProjectRoadmap project={project} />

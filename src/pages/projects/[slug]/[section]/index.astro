---
import ProjectDoc from '../../../../components/pages/ProjectDoc.astro';
import { docPaths } from '../../../../lib/projects';

export async function getStaticPaths() {
  return docPaths('en');
}

const { project, doc } = Astro.props;
---
<ProjectDoc project={project} doc={doc} />

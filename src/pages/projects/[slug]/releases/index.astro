---
import ProjectReleases from '../../../../components/pages/ProjectReleases.astro';
import { sectionPaths } from '../../../../lib/projects';

export async function getStaticPaths() {
  return sectionPaths('en', 'releases');
}

const { project } = Astro.props;
---
<ProjectReleases project={project} />

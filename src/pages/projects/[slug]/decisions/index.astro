---
import ProjectDecisions from '../../../../components/pages/ProjectDecisions.astro';
import { sectionPaths } from '../../../../lib/projects';

export async function getStaticPaths() {
  return sectionPaths('en', 'decisions');
}

const { project } = Astro.props;
---
<ProjectDecisions project={project} />

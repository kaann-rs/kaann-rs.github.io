---
import ProjectChangelog from '../../../../components/pages/ProjectChangelog.astro';
import { sectionPaths } from '../../../../lib/projects';

export async function getStaticPaths() {
  return sectionPaths('en', 'changelog');
}

const { project } = Astro.props;
---
<ProjectChangelog project={project} />

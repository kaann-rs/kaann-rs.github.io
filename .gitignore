dist/
node_modules/
.astro/
.DS_Store
*.log
.env
.env.production

# Obsidian: workspace state is personal, settings are shared
src/content/.obsidian/workspace.json
src/content/.obsidian/workspace-mobile.json
src/content/.obsidian/cache

# Next only hosts the Payload admin locally; its build output is disposable
cms/.next/

# The content database itself IS tracked — it is the source of truth for the
# CMS, and the build needs it. Its journal files are not.
cms/content.db-shm
cms/content.db-wal
